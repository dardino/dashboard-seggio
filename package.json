{
  "name": "referendum2026-workspace",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "dev:client": "turbo run dev --filter=referendum2026-client",
    "dev:server": "turbo run dev --filter=referendum2026-server"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
